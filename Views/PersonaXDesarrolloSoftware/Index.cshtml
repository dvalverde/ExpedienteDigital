@model IEnumerable<ExpDigital.Models.PersonaXDesarrolloSoftware>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.distribucionAutoria)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DesarrolloSoftware.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Persona.cedula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.distribucionAutoria)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DesarrolloSoftware.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Persona.cedula)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
