@model ExpDigital.Models.Jurado

@{
    ViewBag.Title = "CreateJurado";
}


@using (Html.BeginForm("CreateJurado", "Jurado", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()

    <div class="content-container">

        <div class="container-fluid">
            <!--Verifico si hay un error o si se agrego un libro-->
            @if (ViewBag.jurAgregad != null)
            {
                <div class="alert alert-success">
                    <strong>Libro Agregado!</strong> el jurado de premio se agregó satisfactoriamente
                </div>
            }
            @if (ViewBag.errorjura != null)
            {
                <div class="alert alert-danger">
                    <strong>Error!</strong> No se pudo agregar el libro.
                </div>
            }

            <!-- Main component for a primary marketing message or call to action -->
            <div class="jumbotron" style="background-color: #eeeeee">
                <h1>Jurado de Premios Nacionales</h1>
                <br />
                <br />
                <p class="text-info">Complete la información correspondiente</p>
                <br />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <form>
                    <div class="form-row">
                        <div class="form-group col-md-12">
                            <label for="inputEmail4">Nombre del Premio</label>
                            <div>
                                @Html.EditorFor(model => model.nombre, new { htmlAttributes = new { @class = "form-control", @required="required" } })
                                @Html.ValidationMessageFor(model => model.nombre, "", new { @class = "text-danger" })
                            </div>
                        </div>
                    </div>
                    <br />
                    <br />
                    <br />
                    <p class="text-info">Subir PDF:</p>
                    <div class="form-group mt-3 col-md-10">
                        <input type="file" name="upload" required/>
                       
                    </div>

                    <br />
                    <br />
                    <br />
                    <br />

                    <input type="submit" value="Aceptar" class="btn btn-primary" />
                </form>

            </div>

        </div>
    </div>


}


